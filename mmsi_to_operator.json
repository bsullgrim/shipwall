{
  "_comment": "MMSI -> operator key. Grows over time. Keys MUST match firmware sprite keys in ship_sprites.h (e.g. CSL, ALGOMA, FEDNAV, LOWERLAKES, ASC, USESPA). Name-prefix matching in operators.py handles most vessels automatically, so this file is only needed for ships whose names do not fit a prefix rule, or to override one.",
  "338021074": "USESPA",
  "316001234": "CSL"
}